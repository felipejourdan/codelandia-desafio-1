import React from 'react';
import Input from './Input';
import Nav from './Nav';

const Header = () => {
  return (
    <header>
      <Nav />
      <Input />
    </header>
  );
};

export default Header;
